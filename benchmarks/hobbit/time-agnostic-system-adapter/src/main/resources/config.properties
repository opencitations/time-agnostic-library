# Configuration for Time-Agnostic System Adapter

# System endpoint configuration
system.endpoint=http://localhost:8080/sparql

# Data storage configuration
data.folder=/versioning/data

# Connection parameters (if needed)
# connection.timeout=30000
# connection.retries=3

# System-specific parameters
# Add your own system's configuration parameters here
# For example:
# your-system.param1=value1
# your-system.param2=value2

# Logging configuration
log.level=INFO 